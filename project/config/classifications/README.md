# Product Classifications

Provide your full product classifications and sub-classes here. These will be used to validate operations (every product must be tagged) and to drive reporting.

Two supported formats:
- YAML (recommended): `product_classifications.yaml`
- CSV: `product_classifications.csv` (columns: Category, SubCategory, Notes)

No data is included by default. Fill one of the formats and commit.
